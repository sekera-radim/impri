node_modules/
dist/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
*.db
*.db-shm
*.db-wal
.env
.env.local

# Internal dev / build / launch / research docs — never commit.
# These are AI-generated working notes and business/strategy docs kept locally
# only. Public product docs (docs/*.md that build impri.dev/docs), the root
# README and component READMEs stay tracked.
/ARCHITECTURE.md
/MONETIZATION.md
/PLAYBOOK.md
/SPEC.md
/REVIEW-*.md
/docs/launch/
/docs/research-*.md

# Internal SEO content engine (topic queue, generation brief) — kept local only.
# The generated docs/*.md pages ARE public; only the machinery stays private.
/scripts/seo/
# Internal outreach engine (Reddit find & reply, Dev.to cross-post) — local only.
/scripts/outreach/
