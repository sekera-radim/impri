{
  "name": "@signoff/mcp",
  "version": "0.1.0",
  "description": "Signoff MCP server — human-in-the-loop approval inbox for AI agents",
  "keywords": ["mcp", "signoff", "human-in-the-loop", "approval", "ai-agent"],
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "signoff-mcp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test",
    "check": "npm run typecheck && npm run lint && npm run test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  }
}
